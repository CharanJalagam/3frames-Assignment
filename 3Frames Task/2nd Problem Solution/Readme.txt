Constraints are give as 10 MB RAM and 1GB text file here as we are reading the file line by line so the program always stays inside the constraints.

The query that is given for Fuzzy search is : Trpa
the output of the Fuzzy Search is "trap" which is present in the file

In this code we are using Treemap to Sort the Words